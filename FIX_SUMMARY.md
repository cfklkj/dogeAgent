# 修复总结 - MiniMax 模型适配

## 问题
- MiniMax 模型输出 `<think>...</think>` 思考过程标签
- 用户看到原始思考内容而不是最终回复

## 已修复
1. ✅ 在 `factory.py` 中添加过滤逻辑
2. ✅ 自动去除 `<think>...</think>` 包裹的内容
3. ✅ 只保留最终回复

## 测试方法
1. 重启 dogeAgent
2. 在聊天框输入任意消息
3. 应该看到干净的回复，没有 `<think>` 标签

## 下一步
重启 dogeAgent 后测试：
- "BTC 什么价格" - 应该调用工具返回价格
- 或者任意对话 - 不应该显示 `<think>` 标签
